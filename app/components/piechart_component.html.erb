<div class="relative inline-block w-48 h-48">
  <canvas data-controller="piechart" 
          data-piechart-url-value="/api/v1/group_statistics/<%= @group_id %>/pie_data"
          data-piechart-group-id-value="<%= @group_id %>"
          id="group_<%= @group_id %>_donut_chart" 
          class="absolute inset-0">
  </canvas>

  <div class="absolute inset-0 flex items-center justify-center">
    <input type="checkbox" class="w-10 h-10 cursor-pointer p-2 rounded-lg">
  </div>
</div>

<script>
  // Remove the existing script and replace with a Stimulus controller
</script> 