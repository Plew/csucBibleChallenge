<div class="container mx-auto px-1">
  <div class="mb-2">
    <%= render DateBarComponent.new(days: @days, selected_date: @selected_date, mobile: @mobile) %>
  </div>
  
  <% if has_reading? %>
    <%= render ReadingDisplayComponent.new(title: @reading_title, verses: @verses) %>

    <div class="flex justify-center mt-4 mb-8">
      <% if @is_completed %>
        <button class="btn btn-outline btn-success" disabled>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
          </svg>
          Marked as Read
        </button>
      <% else %>
        <button class="btn btn-success">
          Mark as Read
        </button>
      <% end %>
    </div>
  <% else %>
    <div class="bg-base-100 shadow-sm rounded-lg p-4 flex flex-col items-center text-center">
      <h2 class="text-lg font-semibold mb-2">No Readings Scheduled</h2>
      <p>There are no readings scheduled for <%= selected_date_formatted %> in the <%= @challenge_name %>.</p>
    </div>
  <% end %>
</div>