.flex.text-sm class="w-1/3"
  = "Last #{last_x_days.size} Days"
#user-last-x.flex.w-full.h-6
  - last_x_days.reverse.each do |date|
    .flex-grow.h-full class="#{check_in_dates.include?(date) ? 'bg-green-800' : 'bg-gray-800'}"