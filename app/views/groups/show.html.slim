h1.text-2xl.font-bold.mb-4 = @group.name

p.mb-4
  strong Name:
  = @group.name

= render(GroupPieChartComponent.new(group: @group))

= link_to 'Back', groups_path, class: "text-blue-500 hover:text-blue-700"