= form_with(url: group_memberships_path, method: :post) do |f|
  .form-group
    = f.label :key, 'Enter Group Key'
    = f.text_field :key, class: 'form-control', required: true, pattern: '^[A-Za-z]{4}-[0-9]{4}$', title: 'Please enter 4 letters, a hyphen, followed by 4 numbers', maxlength: 9

  = f.submit 'Join Group', class: 'btn btn-primary mt-3'

javascript:
  document.addEventListener('DOMContentLoaded', function() {
    var keyInput = document.querySelector('input[name="key"]');
    keyInput.addEventListener('input', function(e) {
      var value = e.target.value.toUpperCase();
      var letters = value.slice(0, 4).replace(/[^A-Z]/g, '');
      var numbers = value.slice(5).replace(/[^0-9]/g, '');
      if (letters.length === 4 && value.length > 4 && value[4] !== '-') {
        e.target.value = letters + '-' + numbers;
      } else {
        e.target.value = letters + (letters.length === 4 ? '-' : '') + numbers;
      }
    });

    keyInput.addEventListener('keydown', function(e) {
      if (e.key === 'Backspace' && this.value.length === 5 && this.value[4] === '-') {
        e.preventDefault();
        this.value = this.value.slice(0, 4);
      }
    });

    document.querySelector('form').addEventListener('submit', function(e) {
      e.preventDefault();
      this.submit();
    });
  });

