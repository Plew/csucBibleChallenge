<div class="card bg-base-100 shadow-xl max-w-2xl">
  <div class="card-body">
    <%= form_with(model: [:admin, @challenge, @sprint], local: true, class: "space-y-4") do |form| %>
      <% if @sprint.errors.any? %>
        <div class="alert alert-error">
          <h2 class="font-bold"><%= pluralize(@sprint.errors.count, "error") %> prohibited this sprint from being saved:</h2>
          <ul class="list-disc list-inside">
            <% @sprint.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-control">
        <%= form.label :title, t("admin.sprints.name"), class: "label" %>
        <%= form.text_field :title, class: "input input-bordered w-full", placeholder: t("admin.sprints.name_placeholder") %>
      </div>

      <div class="form-control">
        <%= form.label :begin_date, t("admin.sprints.begin_date"), class: "label" %>
        <%= form.date_field :begin_date, class: "input input-bordered w-full", min: @challenge.start_date, max: @challenge.end_date %>
        <label class="label">
          <span class="label-text-alt"><%= t("admin.sprints.begin_date_hint", start_date: @challenge.start_date.strftime("%B %d, %Y")) %></span>
        </label>
      </div>

      <div class="form-control">
        <%= form.label :end_date, t("admin.sprints.end_date"), class: "label" %>
        <%= form.date_field :end_date, class: "input input-bordered w-full", min: @challenge.start_date, max: @challenge.end_date %>
        <label class="label">
          <span class="label-text-alt"><%= t("admin.sprints.end_date_hint", end_date: @challenge.end_date.strftime("%B %d, %Y")) %></span>
        </label>
      </div>

      <div class="card-actions justify-end mt-6">
        <%= link_to t("common.cancel"), admin_challenge_path(@challenge), class: "btn btn-outline" %>
        <%= form.submit t("common.save"), class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
