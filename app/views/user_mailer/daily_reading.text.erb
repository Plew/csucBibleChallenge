Good morning, <%= @user.username %>!

Click below to log in and read today's <%= (@readings&.size.to_i > 1) ? 'chapters' : 'reading' %>: <%= @reading_title %>.

<%= email_login_url(@login_token.token) %>

Thanks,
The And God Said Team
